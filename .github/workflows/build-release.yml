name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., 1.0.0.0)'
        required: true
        default: '1.0.0.0'

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v1.3
      
    - name: Setup NuGet
      uses: nuget/setup-nuget@v1
      
    - name: Restore NuGet packages
      run: nuget restore OpenAdhanForWindowsX.sln
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'
      
    - name: Install WiX Toolset v6
      run: |
        Write-Host "Installing WiX Toolset v6.0.2..."
        dotnet tool install --global wix --version 6.0.2
        
        Write-Host "WiX installed successfully!"
        wix --version
      shell: powershell
      
    - name: Determine version
      id: version
      run: |
        if ("${{ github.event_name }}" -eq "push") {
          $version = "${{ github.ref }}" -replace 'refs/tags/v', ''
        } else {
          $version = "${{ github.event.inputs.version }}"
        }
        echo "VERSION=$version" >> $env:GITHUB_ENV
        echo "version=$version" >> $env:GITHUB_OUTPUT
      shell: powershell
      
    - name: Build Solution
      run: |
        msbuild OpenAdhanForWindowsX.sln /p:Configuration=Release /p:Platform="Any CPU" /t:Rebuild /v:minimal
      
    - name: Build Installer
      run: |
        .\build-installer.ps1 -Configuration Release -Version "${{ env.VERSION }}"
      shell: powershell
      
    - name: List installer output directory
      run: |
        Write-Host "===== Listing OpenAdhanInstaller\bin contents =====" -ForegroundColor Cyan
        if (Test-Path "OpenAdhanInstaller\bin") {
          Get-ChildItem -Path "OpenAdhanInstaller\bin" -Recurse | ForEach-Object {
            Write-Host "$($_.FullName) - $($_.Length) bytes"
          }
        } else {
          Write-Host "OpenAdhanInstaller\bin directory does not exist!" -ForegroundColor Red
        }
      shell: powershell
        
    - name: Find MSI file
      id: find_msi
      run: |
        $msi = Get-ChildItem -Path "OpenAdhanInstaller\bin" -Filter "*.msi" -Recurse -ErrorAction SilentlyContinue | Select-Object -First 1
        if ($msi) {
          Write-Host "Found MSI: $($msi.FullName)"
          # Convert to relative path for GitHub Actions
          $relativePath = $msi.FullName.Replace("$pwd\", "").Replace("\", "/")
          Write-Host "Relative path: $relativePath"
          echo "MSI_PATH=$relativePath" >> $env:GITHUB_ENV
          echo "msi_path=$relativePath" >> $env:GITHUB_OUTPUT
        } else {
          Write-Error "MSI file not found in OpenAdhanInstaller\bin!"
          exit 1
        }
      shell: powershell
      
    - name: Upload Installer Artifact
      uses: actions/upload-artifact@v4
      with:
        name: OpenAdhanSetup-${{ steps.version.outputs.version }}
        path: ${{ steps.find_msi.outputs.msi_path }}
    
    - name: Verify MSI exists
      shell: powershell
      env:
        MSI_FILE_PATH: ${{ env.MSI_PATH }}
      run: |
        $msiPath = $env:MSI_FILE_PATH
        if (Test-Path $msiPath) {
          Write-Host "MSI file verified: $msiPath"
          $size = (Get-Item $msiPath).Length / 1MB
          Write-Host "Size: $([math]::Round($size, 2)) MB"
        } else {
          Write-Error "MSI file does not exist at path"
          exit 1
        }
    
    - name: Create Release
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: ${{ steps.find_msi.outputs.msi_path }}
        draft: false
        prerelease: false
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
